// Retos Opcionales

// Reto 1
// Crea una nueva clase llamada Vector con un único atributo que es un array de números y
// los siguiente métodos:

// 1. Constructor(n, m): Crea un array de n números aleatorios que van desde 0 hasta m.

// 2. add(a): Suma el array a con el array de la clase vector si y solo si tienen el mismo
// numero de elementos.

// 3. productNum(n): Multiplica el array de la clase vector por el numero n.

// 4. subs(a): Resta el array a con el array de la clase vector si y solo si tienen el mismo
// numero de elementos.

// 5. product(a): Multiplica el array a con el array de la clase vector si y solo si tienen el
// mismo número de elementos.

// Crea un nuevo fichero que llame testVector y prueba la clase Vector.

// Reto 2
// Crea una nueva clase llamada Matrix con un único atributo que es un array de objetos de la
// clase Vector y los siguiente métodos:

// 1. Constructor(n, m, k): Crea una matriz de nxm de números aleatorios que van desde 0
// hasta k.

// 2. add(m): Suma la matriz m con la matriz de la clase Matrix si y solo si tienen el mismo
// numero de elementos.

// 3. productNum(n): Multiplica la matriz de la clase Matrix por el numero n.

// 4. subs(m): Resta la matriz m con la matriz de la clase Matrix si y solo si tienen el mismo
// número de elementos.

// 5. product(a): Multiplica la matriz m con la matriz de la clase Matrix si y solo si tienen el
// mismo número de elementos.

// Crea un nuevo fichero que llame testMatrix y prueba la clase Matrix.